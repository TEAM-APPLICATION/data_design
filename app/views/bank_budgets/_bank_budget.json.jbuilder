json.extract! bank_budget, :id, :work_program_group, :work_program, :unit, :fiscal_year, :bank_budget, :all_funds, :notes, :created_at, :updated_at
json.url bank_budget_url(bank_budget, format: :json)
